package com.blogappapis.utilities;

public class AppConstants {

	// Constants is used in all over the application using name of field.
	// it is a best practice to write your constants variable in application.
	// instead use of default value you can use constants variable and it is
	// repeatable use.
	// constants fields should be in capital letters.
	// its a static field so you can use along with class name. 
	// Use -> defaultValue = AppConstants.PAGE_NUMBER;

	public static final String PAGE_NUMBER = "0";
	public static final String PAGE_SIZE = "6";
	public static final String SORT_BY = "postId";

}
